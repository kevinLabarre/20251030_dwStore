1. Installation json-server 
	-> npm i json-server

2. Créer un fichier db.json à la racine du projet
	y mettre au moins un jeu de donnée

3. Ajouter la commande suivante aux scripts dans le package.json: 
	"json-server": "json-server --watch --port 3001 db.json"